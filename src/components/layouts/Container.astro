---
export interface Props {
  size?: "sm" | "md" | "lg";
  class?: string;
}

const {
  size = "md",
  class: className
} = Astro.props;
---

<div class:list={["container", `container--${size}`, className]}>
  <slot />
</div>