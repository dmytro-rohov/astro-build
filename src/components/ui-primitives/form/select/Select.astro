---
import "./select.scss";

interface Props {
  name?: string
  disabled?: boolean
}

const {
  name,
  disabled = false
} = Astro.props;
---

<select
  class="select"
  name={name}
  disabled={disabled}
>

  <slot />

</select>